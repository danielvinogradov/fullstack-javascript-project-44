/**
 * Default config for Brain Even Game.
 */
const BrainEvenDefaultConfig = {
  /**
   * Minimum number that can be generated (inclusive).
   */
  MIN_GENERATED_NUMBER: 0,

  /**
   * Maximum number that can be generated (exclusive).
   */
  MAX_GENERATED_NUMBER: 100,

  /**
   * Number of rounds a player need to win in order to win the game.
   */
  MAX_ROUNDS: 3,
};

export default BrainEvenDefaultConfig;
